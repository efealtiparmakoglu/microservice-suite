apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microservice-suite-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: microservice-suite.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: microservice-suite-service
            port:
              number: 80
